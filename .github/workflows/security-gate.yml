name: Security Gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - name: Repo auschecken
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Python einrichten
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.11"

      - name: Node einrichten
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: "22"

      - name: Getrackte Secrets blockieren
        run: python scripts/security_scan.py

      - name: Security-Code Syntax pruefen
        run: |
          python -m py_compile scripts/security_scan.py scripts/universe_regression.py
          node --check shop-worker/notifications.js
          node --check shop-worker/game-rewards.js
          node --check shop-worker/worker-entry.js
          node --check shop-worker/backend-runtime.js
          node --check shop-worker/worker.js
          node --check assets/game-rewards.js
          node --check assets/ios-zoom-lock.js
          node --check assets/universe-upgrade.js

      - name: Backend-Regressionen pruefen
        run: node --test shop-worker/notifications.test.mjs shop-worker/game-rewards.test.mjs

      - name: Coupon-Schema pruefen
        run: |
          { echo "CREATE TABLE commerce_orders (id TEXT PRIMARY KEY, status TEXT NOT NULL);"; cat shop-worker/migrations/0009_game_rewards_coupons.sql; } | sqlite3 :memory:

      - name: Universum-Mobile-Regressionen pruefen
        run: python scripts/universe_regression.py
